# 禅道产品归属闸门诊断

- 症状：禅道需求只按编号获取，目标代码库无法判断需求是否属于自身产品。
- 根因：`ZenTaoRequirementSource.fetch()` 保留了 product id，但没有查询产品详情或与项目配置比对。
- 影响：同一账号跨多个禅道产品时，错误需求可能继续进入分析、开发和审查阶段。
- 修复边界：仅在 `project.zentao_product` 非空时查询产品 code 并精确匹配；未配置时保持原有请求和行为。
